1. the name and avatar icon are not centered. If you look at the space between the flower and the top, that is much less space than between the bottom of the text and the bottom of the circle. The same thing for the top of the moon and the bottom of the username text. Those should be shifted down and they should be centered so that the top of the icon has the same amount of space as underneath the text to the bottom of the circle. 

2. On this page, instead of the log out text underneath the users, there should just already be the user icon in the top right that you could do settings or log out, etc. You wouldn't switch user there, but you would do the other things there. 

3. When the add user button is clicked, the avatar is not in an even grid. It needs to have an even space. It should be square. Instead of all the space in between, the space that is in between each of them up and down, the space that is in between them side to side should be reduced to match the space that's in between them top to bottom. 

4. Maybe instead of the user nomenclature, maybe we use profile. So instead of "add user", it would be "add profile", and instead of "new user with the name", we would do "new profile with the name". 

5. The name box should be the same size as the create button. Then, instead of the cancel text, that should be an icon in the top right of that modal with a little X in the middle of it that highlights when you click it. 

6. The browser back button doesn't work when I press it. Let's say I log into a user or select a user profile to play as, then I press the back button on my browser. It does not let me go back to choosing the profile. 

7. In the top right, where it's profile, we should have a delete profile option. It should pop a confirmation modal. Then, instead of a hard delete of the profile, we should just make it a soft delete. That way, if we want to make a manual edit and get the profile back online, it's very easy, and we won't lose any progress, etc. 

8. When a song or a MIDI file is dropped in and loaded, it should save it to the database for future reference. It should do an analysis to assign a difficulty level to it.

9. The song selection page should be changed. It should show 'recent songs' by default, then maybe we have a find additional songs link/button, etc... that then loads a browse experience. Which will then have the search feature, in addition to a browse functionality which has different filters (difficulty, genre, tempo, etc.) this page would also have the load custom song on it, not the main page. 

10. the profile avatar should show in the top right instead of the placeholder 'human' outline once the profile is selected.

11. What determins left hand and right hand? check the LR Hand mixup photo. There is a note which clearly appears to belong to the left hand, but which is showing as belonging to the right hand. Is there anything we can do about that? Is that in the MIDI file? Is there a script or tool which we can find or build to scan and fix the MIDI files? Or can we add a context menu when right clicking on the waterfall note? Then it would give additional options. One could be changing the color for the hand. Another could be changing which hand that particular note belongs to? Maybe we do all of the above?

12. What is the feasibility of click and dragging the waterfall to change the location in the song? Similar to just scrubbing the progress bar at the top

13. The musical notes notation is incorrect. You can see that the quarter notes are not displaying correctly. The same thing with the smaller syncopated notes. I'm not sure what we can do about improving how it is displayed, but that would be helpful to have it displayed correctly. see the 'musical notation' pic

14. We should be able to deselect the learned property. 

15. Like the idea of being able to merge sections to give them names, etc. This can be a future feature, but something along the lines of being able to manage the sections. And then maybe it gives you the ability to isolate a section so you can toggle sections on and off to practice. When you press play, it doesn't just play the whole song; it will just work on that section if you have that section acor that tive. I also kind of like the idea of having the "practiced" designation as something that is physically marked, similar to the "learned" being physically marked. The "practice" mode can be toggled on and off, or maybe it goes through different stages, like maybe you toggle it once and that turns from the default state. Maybe there is not even really that new, like something along the lines of the base or neutral or default, something like that, the starting state. It then goes to "practice" and then it goes to "learned". I'd like to do some more brainstorming on this whole sections part of the app Before we get too far on making any changes. I think that will make the sections thing in the top bar irrelevant and we could remove them.

  